# TODO

## Фронтенд

### 1. Маршруты — UI
- [x] Кнопка «+ Маршрут» на карте (в режиме админа)
- [x] Модалка выбора маршрута: пункт А → Б (+ добавление промежуточных точек В, Г…)
- [x] Выбор вида транспорта: самолёт, поезд, автобус, водный транспорт
- [x] После сохранения — POST на бэкенд, затем маршрут появляется на карте
- [x] Загрузка маршрутов с `GET /api/routes` при старте приложения
- [x] Отрисовка маршрутов с бэкенда на карте (пунктирная линия по цвету транспорта)

### 2. Загрузка фото в модалке добавления места
- [x] Инпут `<input type="file">` с поддержкой множественного выбора
- [x] Превью выбранных фото перед отправкой
- [ ] Отображение сохранённых фото в виде кликабельной карусели *(уже есть в PlaceModal)*

### 4. Редактирование места — категория
- [x] В режиме редактирования добавить чекбоксы для изменения категории

### 5. Редактирование места — фотографии
- [x] Возможность удалять отдельные фото
- [x] Загрузка новых фото (URL) в режиме редактирования
- [x] Кнопки ↑/↓ для изменения порядка фотографий

---

## Бэкенд

### 1. Маршруты — API
- [x] Таблица `routes` в SQLite
- [x] `GET /api/routes` — список всех маршрутов
- [x] `POST /api/routes` — создание маршрута (авторизация по токену)
- [x] `DELETE /api/routes/:id` — удаление маршрута (X-Admin-Token header)

### 3. Загрузка фото
- [x] `POST /api/photos` — принимает `multipart/form-data`, сохраняет в `server/data/uploads/`
- [x] `GET /uploads/:filename` — отдача загруженных файлов
- [x] Возврат URL загруженных фото в ответе

### 5. Редактирование фото на бэкенде
- [x] `POST /api/places` (upsert) — обновление места вместе с новым списком фото и категорий

---

## Ещё идеи / бэклог

- [ ] Удаление маршрута через UI (кнопка в модалке или на карте)
- [ ] Редактирование существующего маршрута
- [ ] Загрузка новых файлов в режиме редактирования фото в PlaceModal (сейчас только URL)
- [ ] Перенести `TRAVEL_STORY_LEGS` из хардкода в БД
